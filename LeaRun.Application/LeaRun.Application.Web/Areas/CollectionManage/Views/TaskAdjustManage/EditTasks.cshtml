@{
    ViewBag.Title = "采集任务期限调整";
    Layout = "~/Views/Shared/_Form.cshtml";
}

<script>
    var keyValue = request('keyValue');
    $(function () {

    });

    //保存表单
    function AcceptClick() {
        debugger;
        if (!$('#form1').Validform()) {
            return false;
        }
       // var postData = $("#form1").GetWebControls(keyValue);
        var taskIds = $.currentIframe().getSelectTaskIds();
        var postData = { taskIds: String(taskIds), startDate: $("#KSSJ").val(),endDate: $("#JZSJ").val()};
        debugger;
        $.SaveForm({
            url: "../../CollectionManage/TaskAdjustManage/AdjustTaskDates",
            param: postData,
            loading: "正在保存数据...",
            success: function() {
                $.currentIframe().$("#gridTable").trigger("reloadGrid");
            }
        });
        return false;
    }
</script>
<div class="">
    <div style="margin-top: 20px; margin-right: 30px;">
        <table class="form" style="border: 1px solid #ccc; margin-left: 15px;margin-top: 20px">
            <tr>
                <td class="formTitle">开始时间</td>
                <td class="formValue">
                    <input id="KSSJ" isvalid="yes" checkexpession="NotNull" type="text" class="form-control input-wdatepicker" onfocus="WdatePicker()" />
                </td>
            </tr>
            <tr>
                <td class="formTitle">截止时间</td>
                <td class="formValue">
                    <input id="JZSJ" type="text"  isvalid="yes" checkexpession="NotNull" class="form-control input-wdatepicker" onfocus="WdatePicker()" />
                </td>
            </tr>
        </table>
    </div>
</div>
